control_mec <- function(comp_rate = 0.9,
                        deduplication = TRUE,
                        add_xy = FALSE,
                        maxit = 100){


  list(comp_rate = comp_rate,
       deduplication = deduplication,
       add_xy = ass_xy,
       maxit = maxit)
}
